# --- Secrets / local config ---
# Chặn mọi biến thể .env (real secrets), trừ template .example và frontend/.env vô hại
.env
.env.*
*.env
*.env.local
*.bak
!*.env.example
!.env.example

# --- Python ---
__pycache__/
*.py[cod]
backend/venv/
*.egg-info/

# --- Database / runtime data ---
backend/app.db
backend/app.db-wal
backend/app.db-shm
backend/storage/
backend/cache/
backend/logs/
backend/stickers/
backend/fonts/
logs/
*.log

# --- Bundled binaries (large; provide separately) ---
backend/bin/ffmpeg.exe
backend/bin/ffprobe.exe

# --- Build artifacts ---
frontend/node_modules/
frontend/dist/
build/
dist/
dist_new/
packaging/Output/
*.spec.bak

# --- OS / editor ---
.DS_Store
Thumbs.db

# OmniVoice models (downloaded at install, not committed)
backend/models/
backend/clone_presets/

# Source corpus for seed_clone_presets_from_vivos.py (~1.4GB, download locally)
backend/scripts/vivos_data/

# Standalone OmniVoice reference repo (nested git repo + multi-GB weights) —
# kept locally for reference, never part of this repo.
omivoice-tts/

omivoice/
