# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
ENV/

# Environment
.env
.env.local

# Storage
storage/*/
!storage/.gitkeep

# IDE
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log
logs/

# Linux ffmpeg/ffprobe fetched at build time by the Ubuntu CI (never commit)
bin_linux/

# Database
*.db
*.sqlite
*.sqlite3
# ...except the shipped reference DB (curated banned words + prompts, no stories).
# It's a real build artifact bundled into the .exe — see packaging/make_seed_db.py.
!default_seed.db
