# FastAPI
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Database (SQLite via stdlib sqlite3 — no external driver needed)
sqlalchemy==2.0.27

# Existing dependencies (refactored)
requests==2.31.0
beautifulsoup4==4.12.3
chardet==5.2.0
tabulate==0.9.0

# Audio/image processing
ffmpeg-python==0.2.0
Pillow>=11.0.0  # >=11 has wheels for Python 3.13

# Desktop app (native window via Windows WebView2)
pywebview>=6.0
# On Windows pywebview's EdgeChromium backend needs pythonnet
# (>=3.0.5 has Python 3.13 support)
pythonnet>=3.0.5; sys_platform == "win32"

# Utils
pydantic==2.5.3
pydantic-settings==2.1.0
python-dotenv==1.0.0
loguru==0.7.2
tenacity==8.2.3

# File handling
aiofiles==23.2.1

# Document export
python-docx==1.1.0
